doctype html
html
  head
    if !error
      title= title
      link(rel='stylesheet', href='/stylesheets/style.css')
    if error
      style. 
        :root {
          background-color: #000;
          color: #e3e3e3;
          font-family: monospace;
          font-size: larger;
          font-weight: bolder;
          text-align: center;
        }
        .main {
          height: 100vh;
          display: grid;
          place-items: center;
        }
        html body {
          padding: 0;
          margin: 0;
        }
            
  body
    block content
